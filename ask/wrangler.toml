name = "clicarity-chat"
main = "chat-worker.js"
compatibility_date = "2024-01-01"

# Your API key is stored as a secret — never in this file
# After deploying, run: wrangler secret put ANTHROPIC_API_KEY
# Then paste your key when prompted

[vars]
# Non-secret variables go here (none needed for now)
